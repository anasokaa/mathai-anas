{
  "name": "mathai-anas",
  "version": "1.0.0",
  "description": "A cyberpunk-themed math equation solver by Anas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
